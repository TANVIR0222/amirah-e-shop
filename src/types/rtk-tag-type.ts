// interface/tag-types.tsx
export enum tagTypes {
  profile = "profile",
  auth = "auth",
  category = "category",
  // product = "product",
}

export const tagTypesList = [tagTypes.profile, tagTypes.auth, tagTypes.category]
